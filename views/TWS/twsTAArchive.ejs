<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>TWS - TA Archive</title>
  <link rel="stylesheet" href="/TWS/css/twsstyles.css" />
</head>
<body>
  <%- include('../partials/sideMenuBar') %>

  <div class="main">
    <div class="shell">

      <div class="page-title">TWS - TA Archive</div>

      <!-- Approved Teaching Workload Sheets -->
      <div class="panel">
        <div class="panel-header">
          <div class="panel-title">Approved Teaching Workload Sheets</div>
          <div class="panel-actions">
            <div class="search-wrap">
              <input class="search-bar" type="text" id="taSearch" placeholder="Search by Name" oninput="filterTA()" />
              <span class="search-icon">&#128269;</span>
            </div>
            <button class="btn btn-purple" type="button">&#9965; Filter by Department</button>
            <a class="btn" href="/tws/ta-archive">Refresh</a>
          </div>
        </div>

        <div class="tws-table col-ta" id="taTable">
          <div class="trow thead">
            <div>Faculty Name</div>
            <div>Department</div>
            <div>Academic Year</div>
            <div>Term</div>
            <div>Date Approved</div>
            <div>Archived Date</div>
            <div class="tcell-right">Action</div>
          </div>

          <% if (!list || !list.length) { %>
            <div class="trow">
              <div style="grid-column:1/-1;opacity:.7;padding:10px 0;">No approved TWS records yet.</div>
            </div>
          <% } %>

          <% (list || []).forEach(function(t) {
               var approvedDate = t.approval && t.approval.approvedAt
                 ? new Date(t.approval.approvedAt).toLocaleDateString('en-US',{month:'2-digit',day:'2-digit',year:'numeric'})
                 : new Date(t.createdAt).toLocaleDateString('en-US',{month:'2-digit',day:'2-digit',year:'numeric'});
               var archivedDate = t.archivedAt
                 ? new Date(t.archivedAt).toLocaleDateString('en-US',{month:'2-digit',day:'2-digit',year:'numeric'})
                 : approvedDate;
          %>
            <div class="trow ta-row" data-name="<%= (t.faculty.name || '').toLowerCase() %>"
                 onclick="showArchiveDetail('<%= t.faculty.name || '(Name)' %>', '<%= t.id %>', '<%= t.faculty.acadYear || '2025 - 2026' %>')"
                 style="cursor:pointer;">
              <div class="arch-name"><%= t.faculty.name || '—' %></div>
              <div><%= t.faculty.dept || '—' %></div>
              <div><%= t.faculty.acadYear || '2025- 2026' %></div>
              <div><%= t.faculty.term || '—' %></div>
              <div><%= approvedDate %></div>
              <div><%= archivedDate %></div>
              <div class="tcell-right">
                <a class="pill pill-view" href="/tws/summary/<%= t.id %>">View</a>
              </div>
            </div>
          <% }) %>
        </div>
      </div>

      <!-- Archive Details -->
      <div class="panel" id="archiveDetail">
        <div class="panel-title">Archive Details</div>
        <hr class="divider" />
        <div class="archive-details">
          <div>Selected Faculty: <span id="detailFaculty">(Name)</span></div>
          <div>File Name: <span id="detailFile">(File Name)</span></div>
          <div>Storage Location: HR Records &gt; TWS &gt; SY 2025 - 2026</div>
          <div class="muted">Archive files are stored for record-keeping and retrieval.</div>
        </div>
      </div>

    </div>
  </div>

  <script>
    function filterTA() {
      var q = document.getElementById('taSearch').value.toLowerCase();
      document.querySelectorAll('.ta-row').forEach(function(row) {
        row.style.display = row.dataset.name.includes(q) ? '' : 'none';
      });
    }

    function showArchiveDetail(name, id, sy) {
      document.getElementById('detailFaculty').textContent = name;
      document.getElementById('detailFile').textContent = 'TWS-' + id + '.pdf';
      document.querySelector('#archiveDetail .archive-details div:nth-child(3)').innerHTML =
        'Storage Location: HR Records &gt; TWS &gt; SY ' + sy;
    }
  </script>
</body>
</html>
