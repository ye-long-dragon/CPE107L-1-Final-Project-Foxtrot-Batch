<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>TWS - Summary And Preview</title>
  <link rel="stylesheet" href="/TWS/css/twsStyle.css" />
</head>
<body class="bg">
  <%- include('../partials/sideMenuBar') %>

  <div class="main">
    <div class="shell">
      <div class="page-title">TWS - Summary And Preview</div>

      <form class="panel summary" method="POST" action="/twa/summary/<%= tws.id %>">
        <div class="summary-box">
          <div class="summary-left">
            <div class="panel-title">Faculty Information</div>
            <div class="kv">
              <div>Faculty Name:</div><div><%= tws.faculty.name || "—" %></div>
              <div>Employee ID:</div><div><%= tws.faculty.empId || "—" %></div>
              <div>Department:</div><div><%= tws.faculty.dept || "—" %></div>
              <div>Academic Year:</div><div><%= tws.faculty.acadYear || "—" %></div>
              <div>Term:</div><div><%= tws.faculty.term || "—" %></div>
              <div>Employment Status:</div><div><%= tws.faculty.empStatus || "—" %></div>
            </div>

            <div class="panel-title mt">Teaching Load Summary</div>
            <div class="kv two-col">
              <div>Total Units:</div><div><%= tws.totals.totalUnits %></div>
              <div>Total Hours (Lec/Lab):</div><div><%= tws.totals.totalHours %></div>
              <div>Equivalent Load:</div><div><%= tws.totals.equivLoad %></div>
              <div>Equivalent Non-Teaching Load:</div><div>0</div>
            </div>
          </div>

          <div class="summary-right">
            <button class="btn btn-green btn-stack" type="submit" name="action" value="submit">Submit TWS</button>
            <button class="btn btn-stack" type="submit" name="action" value="edit">Edit</button>
          </div>
        </div>
      </form>
    </div>
  </div>
</body>
</html>